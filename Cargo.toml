[package]
name = "mitepc"
version = "0.1.0"
edition = "2021"
description = "A PC simulator running MiteOS with custom RAM, CPU, and Storage"
authors = ["TheOnly-Coder"]

[dependencies]
crossterm = "0.28"
chrono = "0.4"

[profile.release]
opt-level = 2
lto = true
